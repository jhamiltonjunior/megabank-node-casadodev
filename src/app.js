const express = require('express')
// const cors = require('cors')

const router = require()

const app = express()

// app.use(cors())
app.use(router)

app.listen(5000, () => {
  console.log('OK!')
})
